const VoiceControl = {
// Function to handle the connection to the live view
// We likely dont need this function as we can directly connect via webRTC in the live view
  };
  
  export default AudioCall;